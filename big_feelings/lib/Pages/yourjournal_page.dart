import 'package:flutter/material.dart';

class YourJournalPage extends StatelessWidget {
  const YourJournalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Journal page'),
      ),
    );
  }
}
